import 'package:flutter_test/flutter_test.dart';

// Los tests de integración se añadirán por módulo en fases siguientes.
// Por ahora este archivo existe para que el proyecto no tenga errores de lint.

void main() {
  test('placeholder', () {
    expect(1 + 1, equals(2));
  });
}
